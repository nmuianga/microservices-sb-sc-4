spring:
  application:
    name: product-service

server:
  port: 7002
  error:
    include-message: always

logging:
  level:
    root: INFO
    mz.co.muianga: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker

server:
  port: 8080
