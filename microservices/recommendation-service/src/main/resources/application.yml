spring:
  application:
    name: recommendation-service
  config:
    activate:
      on-profile: docker

server.port: 7003
server.error.include-message: always

logging:
  level:
    root: INFO
    mz.co.muianga: DEBUG
---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080
